diff --git a/common/arg.cpp b/common/arg.cpp
index 49b3380..c6055e2 100644
--- a/common/arg.cpp
+++ b/common/arg.cpp
@@ -75,6 +75,55 @@ static std::string read_file(const std::string & fname) {
     return content;
 }
 
+static std::string parse_sampling_defaults_by_reasoning(const std::string & value) {
+    const auto config = json::parse(value);
+    if (!config.is_object() || config.size() != 2 ||
+        !config.contains("thinking") || !config.contains("non_thinking")) {
+        throw std::invalid_argument(
+            "sampling defaults must contain exactly thinking and non_thinking objects");
+    }
+
+    static const std::set<std::string> fields = {
+        "temperature", "top_p", "top_k", "min_p",
+        "presence_penalty", "repeat_penalty",
+    };
+    for (const char * mode : {"thinking", "non_thinking"}) {
+        const auto & defaults = config.at(mode);
+        if (!defaults.is_object() || defaults.empty()) {
+            throw std::invalid_argument(
+                string_format("sampling defaults mode '%s' must be a non-empty object", mode));
+        }
+        for (const auto & item : defaults.items()) {
+            if (fields.find(item.key()) == fields.end()) {
+                throw std::invalid_argument(
+                    string_format("unsupported sampling default '%s'", item.key().c_str()));
+            }
+            const auto & candidate = item.value();
+            if (item.key() == "top_k") {
+                if (!candidate.is_number_integer() ||
+                    candidate.get<double>() < 0.0 || candidate.get<double>() > INT_MAX) {
+                    throw std::invalid_argument("top_k sampling default must be a non-negative integer");
+                }
+                continue;
+            }
+            if (!candidate.is_number()) {
+                throw std::invalid_argument(
+                    string_format("sampling default '%s' must be numeric", item.key().c_str()));
+            }
+            const double number = candidate.get<double>();
+            if (!std::isfinite(number) ||
+                (item.key() == "temperature" && number < 0.0) ||
+                ((item.key() == "top_p" || item.key() == "min_p") &&
+                 (number < 0.0 || number > 1.0)) ||
+                (item.key() == "repeat_penalty" && number <= 0.0)) {
+                throw std::invalid_argument(
+                    string_format("invalid sampling default '%s'", item.key().c_str()));
+            }
+        }
+    }
+    return config.dump();
+}
+
 static const std::vector<common_arg> & get_common_arg_defs() {
     static const std::vector<common_arg> options = [] {
         common_params params;
@@ -3490,6 +3539,13 @@ common_params_context common_params_parser_init(common_params & params, llama_ex
             }
         }
     ).set_examples({LLAMA_EXAMPLE_SERVER, LLAMA_EXAMPLE_CLI}).set_env("LLAMA_ARG_CHAT_TEMPLATE_KWARGS"));
+    add_opt(common_arg(
+        {"--sampling-defaults-by-reasoning"}, "JSON",
+        "sets fallback sampler fields for thinking and non-thinking chat requests; explicit request fields take precedence",
+        [](common_params & params, const std::string & value) {
+            params.sampling_defaults_by_reasoning = parse_sampling_defaults_by_reasoning(value);
+        }
+    ).set_examples({LLAMA_EXAMPLE_SERVER}).set_env("LLAMA_ARG_SAMPLING_DEFAULTS_BY_REASONING"));
     add_opt(common_arg(
         {"-to", "--timeout"}, "N",
         string_format("server read/write timeout in seconds (default: %d)", params.timeout_read),
diff --git a/common/common.h b/common/common.h
index d8a1689..02ae426 100644
--- a/common/common.h
+++ b/common/common.h
@@ -640,6 +640,7 @@ struct common_params {
     std::string ssl_file_cert = "";                                                                         // NOLINT
 
     std::map<std::string, std::string> default_template_kwargs;
+    std::string sampling_defaults_by_reasoning;
 
     // CLI params
     std::string server_base; // if set, connect to this server instead of starting a new one
diff --git a/tools/server/server-common.cpp b/tools/server/server-common.cpp
index 7ed4865..3429f93 100644
--- a/tools/server/server-common.cpp
+++ b/tools/server/server-common.cpp
@@ -16,6 +16,27 @@
 #include <cstring>
 #include <type_traits>
 
+// Apply data supplied by the model preset only as defaults. This keeps the
+// template responsible for resolving thinking mode without giving Jinja a
+// side channel into sampler state, and explicit request fields remain final.
+static void rocmplete_apply_sampling_defaults(
+        json & llama_params,
+        const json & body,
+        const json & defaults_by_reasoning,
+        bool enable_thinking) {
+    if (defaults_by_reasoning.empty()) {
+        return;
+    }
+    const auto & defaults = defaults_by_reasoning.at(
+        enable_thinking ? "thinking" : "non_thinking");
+    for (const auto & item : defaults.items()) {
+        const auto it = body.find(item.key());
+        if (it == body.end() || it->is_null()) {
+            llama_params[item.key()] = item.value();
+        }
+    }
+}
+
 json format_error_response(const std::string & message, const enum error_type type) {
     std::string type_str;
     int code = 500;
@@ -1370,6 +1391,10 @@ json oaicompat_chat_params_parse(
         }
     }
 
+    rocmplete_apply_sampling_defaults(
+        llama_params, body, opt.sampling_defaults_by_reasoning,
+        inputs.enable_thinking);
+
     return llama_params;
 }
 
diff --git a/tools/server/server-common.h b/tools/server/server-common.h
index 7082abd..8f06ddd 100644
--- a/tools/server/server-common.h
+++ b/tools/server/server-common.h
@@ -298,6 +298,7 @@ struct server_chat_params {
     bool prefill_assistant;
     common_reasoning_format reasoning_format;
     std::map<std::string, std::string> chat_template_kwargs; // mapping key --> json value
+    json sampling_defaults_by_reasoning;
     common_chat_templates_ptr tmpls;
     bool allow_image;
     bool allow_audio;
diff --git a/tools/server/server-context.cpp b/tools/server/server-context.cpp
index fdc8f2b..d32b459 100644
--- a/tools/server/server-context.cpp
+++ b/tools/server/server-context.cpp
@@ -1429,6 +1429,9 @@ private:
                 /* prefill_assistant     */ params_base.prefill_assistant,
                 /* reasoning_format      */ params_base.reasoning_format,
                 /* chat_template_kwargs  */ params_base.default_template_kwargs,
+                /* sampling_defaults     */ params_base.sampling_defaults_by_reasoning.empty()
+                                                ? json::object()
+                                                : json::parse(params_base.sampling_defaults_by_reasoning),
                 /* tmpls                 */ std::move(chat_templates),
                 /* allow_image           */ mctx ? mtmd_support_vision(mctx) : false,
                 /* allow_audio           */ mctx ? mtmd_support_audio (mctx) : false,
